type Query {
    "Get tracks array for homepage grid"
    airWaterQuality: [AirWaterQuality!]!
    airWaterQualitySearch(search: String): [AirWaterQuality!]!
}
    
"A track is a group of Modules that teaches about a specific topic"
type AirWaterQuality {
    city: String!
    airQuality: Float
    country: String
    region: String
    waterPollution: Float
}